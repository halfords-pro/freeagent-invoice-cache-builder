[project]
name = "freeagent-invoice-cache-builder"
version = "0.1.0"
description = "Incrementally download invoices from FreeAgent API"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]
